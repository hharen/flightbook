<% content_for :title, t('.title') %>

<h1><%= t('.heading') %></h1>

<%= render "form", flying_session: @flying_session %>

<br>

<div>
  <%= link_to t('flying_sessions.show.back_link'), flying_sessions_path %>
</div>
