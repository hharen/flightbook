<% content_for :title, t('.title') %>

<h1><%= t('.heading') %></h1>

<%= render "form", instructor: @instructor %>

<%= link_to t('.delete'), instructor_path(instructor),
            data: { "turbo-method": :delete },
            class: "btn btn-small btn-danger" %>

<br>

<div>
  <%= link_to t('.back_link'), instructors_path %>
</div>
